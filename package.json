{
  "name": "vibe-sidecar",
  "version": "0.0.0",
  "description": "Local inspectable workflow-state sidecar for coding agents.",
  "type": "module",
  "bin": {
    "vibe": "./dist/cli.js",
    "vibe-sidecar": "./dist/cli.js"
  },
  "files": [
    "dist",
    "schemas",
    "examples",
    "README.md",
    "LICENSE",
    "LICENSE.schemas"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --dts --sourcemap --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/cli.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "AGPL-3.0-or-later"
}
